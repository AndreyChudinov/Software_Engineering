## Тема 4. Функции и стандартные модули/библиотеки.

### Отчет по Теме #4 выполнил(а):
- Чудинов Андрей Игоревич  
- ИВТ-23-2
## Итоговая таблица выполнения заданий
| Задание      | Лаб_раб | Сам_раб |
|--------------|---------|---------|
| Задание 1    | +       | +       |
| Задание 2    | +       | +       |
| Задание 3    | +       | +       |
| Задание 4    | +       | +       |
| Задание 5    | +       | +       |
| Задание 6    | +       |         |
| Задание 7    | +       |         |
| Задание 8    | +       |         |
| Задание 9    | +       |         |
| Задание 10   | +       |         |

знак "+" — задание выполнено; знак "-" — задание не выполнено;
### Работу проверили:
- к.э.н., доцент Панов М.А.
## Лабораторная работа №1
*Напишите функцию, которая выполняет любые арифметические действия и выводит результат в консоль. Вызовите функцию используя “точку входа”.*
```python
def main():
    print(2+2)


if __name__ == '__main__':
    main()
```
## Результат.
<img width="867" height="403" alt="Image" src="https://github.com/user-attachments/assets/fa9ad6d4-5823-4442-824c-42af7ae517be" />

## Выводы
*Эта работа демонстрирует базовое создание и вызов функции без возвращаемого значения. Функция main() выполняет простое арифметическое действие и выводит результат непосредственно в консоль. Программа показывает структуру простейшей функции и стандартный способ запуска кода через "точку входа". Это фундаментальное упражнение помогает понять базовую структуру программы на Python и принцип разделения определения функции и ее вызова.*

## Лабораторная работа №2
*Напишите функцию, которая выполняет любые арифметические действия, возвращает при помощи return значение в место, откуда вызывали функцию. Выведите результат в консоль. Вызовите функцию используя “точку входа”.*
```python
def main():
    return 2+2


if __name__ == '__main__':
    print(main())
```
## Результат.
<img width="867" height="383" alt="Image" src="https://github.com/user-attachments/assets/37122a56-b18b-4238-927a-8c0dff0cc5f5" />

## Выводы
*Эта работа демонстрирует использование оператора return для возврата значения из функции. В отличие от предыдущей работы, здесь результат арифметической операции возвращается функцией и выводится в месте ее вызова.*

## Лабораторная работа №3
*Напишите функцию, в которую передаются два аргумента, над ними производится арифметическое действие, результат возвращается туда, откуда эту функцию вызывали. Выведите результат в консоль. Вызовите функцию в любом небольшом цикле.*
```python
def main(one, two):
    result = one + two
    return result


for i in range(5):
    x = 1
    y = 10
    answer = main(x, y)
    print(answer)
```
## Результат.
<img width="658" height="373" alt="Image" src="https://github.com/user-attachments/assets/e2242b4a-7287-4363-9a9a-ad3e38358760" />

## Выводы
*Эта работа демонстрирует передачу аргументов в функцию и ее вызов в цикле. Функция main() принимает два параметра, выполняет над ними операцию и возвращает результат. Программа показывает, как функции могут использоваться в итерационных процессах, что расширяет их практическое применение. Это упражнение помогает понять, как функции интегрируются в более сложные структуры программы.*

## Лабораторная работа №4
*Напишите функцию, на вход которой подается какое-то изначальное неизвестное количество аргументов, над которыми будет производится арифметические действия. Для выполнения задания необходимо использовать кортеж "\*args". На скриншоте ниже приведен пример такой программы с комментариями. Для закрепления понимания работы с кортежами настоятельно рекомендуем поменять аргументы вызова функции, вручную посчитать результат, только потом запустить программу с новыми значениями и проверить себя, насколько вы поняли данный аспект программирования.*
```python
def main(x, *args):
    one = x # 10
    two = sum(args) # 0, 1, 2, -1, 0, -1, 1, 2
    three = float(len(args)) # длина кортежа args
    # пример работы операций над параметрами функции
    print(f"one={one}\ntwo={two}\nthree={three}")

    return x + sum(args) / float(len(args))


if __name__ == '__main__':
    result = main(10, 0, 1, 2, -1, 0, -1, 1, 2)
    print(f"\nresult={result}")
```
## Результат.
<img width="974" height="527" alt="Image" src="https://github.com/user-attachments/assets/8c2af743-9eee-4d60-91de-2a3a36ecde19" />

## Выводы
*Эта работа демонстрирует использование специального синтаксиса \*args для передачи переменного числа позиционных аргументов в функцию. Программа показывает, как функция может обрабатывать произвольное количество входных данных, что делает ее более гибкой и универсальной.*

## Лабораторная работа №5
*Напишите функцию, которая на вход получает кортеж “\*\*kwargs” и при помощи цикла выводит значения, поступившие в функцию. На скриншоте ниже указаны два варианта вызова функции с “\*\*kwargs” и два варианта работы с данными, поступившими в эту функцию. Комментарии в коде и теоретическая часть помогут вам разобраться в этом нелегком аспекте. Вызовите функцию используя “точку входа”.*
```python
# оператор ** упаковывает аргументы, переданные по имени, в словарь.
# Имена параметров служат ключами.
def main(**kwargs):
    for i in kwargs.items():
        print(i[0], i[1])

    print() # добавляет пустую строку в консоль

    for key in kwargs:
        print(f"{key} = {kwargs[key]}")


if __name__ == '__main__':
    # обычный вызов функции, принимающий кортеж **kwargs, как параметр
    main(x=[1, 2, 3], y=[3, 3, 0], z=[2, 3, 0], q=[3, 3, 0], w=[3, 3, 0])
    print() # добавляет пустую строку в консоль
    # также возможно использовать в виде аргумента для функции уже готовый
    # словарь, но приписав в начале **
    main(**{'x': [1, 2, 3], 'y': [3, 3, 0]})
```
## Результат.
<img width="904" height="705" alt="Image" src="https://github.com/user-attachments/assets/d8005ba5-849e-4bcf-836e-86ae2dd4d8dd" />

## Выводы
*Эта работа демонстрирует использование специального синтаксиса \*\*kwargs для передачи переменного числа именованных аргументов в функцию. Это упражнение помогает понять, как функции могут принимать и обрабатывать структурированные данные в виде пар "ключ-значение".*

## Лабораторная работа №6
*Напишите две функции. Первая – получает в виде параметра “\*\*kwargs”. Вторая считает среднее арифметическое из значений первой функции. Вызовите первую функцию используя “точку входа” и минимум 4 аргумента.*
```python
def main(**kwargs):
    """Функция работающая с **kwargs."""

    for i, j in kwargs.items():
        print(f"{i}. Mean = {mean(j)}")


def mean(data):
    """Функция подсчета среднего арифметического."""

    return sum(data) / float(len(data))


if __name__ == '__main__':
    main(x=[1, 2, 3], y=[3, 3, 0])
```
## Результат.
<img width="974" height="408" alt="Image" src="https://github.com/user-attachments/assets/e83d4de8-77c0-45d9-b57c-7bd8083e0403" />

## Выводы
*Эта работа демонстрирует комбинацию использования \*\*kwargs и вспомогательной функции для обработки данных. Программа показывает, как можно создавать модульные функции, где одна функция обрабатывает структуру данных, а другая выполняет специфические вычисления. Это упражнение помогает понять паттерн разделения ответственности между функциями и создание более сложных обработчиков данных, что является основой для построения масштабируемых приложений.*

## Лабораторная работа №7
*Создайте дополнительный файл .py. Напишите в нем любую функцию, которая будет что угодно выводить в консоль, но не вызывайте ее в нем. Откройте файл main.py, импортируйте в него функцию из нового файла и при помощи “точки входа” вызовите эту функцию.*
```python
from Tema4_lab7_for_import import say_hello

if __name__ == '__main__':
    say_hello()
```
## Результат.
<img width="974" height="406" alt="Image" src="https://github.com/user-attachments/assets/01e205d7-5c90-4041-922e-d824e42a109c" />

<img width="974" height="278" alt="Image" src="https://github.com/user-attachments/assets/d5dbd850-a7dc-41a0-b278-c3a309b8eeb9" />

## Выводы
*Эта работа демонстрирует импорт функций из других файлов. Программа показывает, как можно организовать код в несколько файлов для лучшей структуризации и повторного использования. Это упражнение помогает понять концепцию модульности в Python, важность правильной организации кода и принципы импорта, что критически важно для разработки крупных приложений.*

## Лабораторная работа №8
*Напишите программу, которая будет выводить корень, синус, косинус полученного от пользователя числа.*
```python
import math


def main():
    value = int(input('Введите значение: '))
    print(math.sqrt(value))
    print(math.sin(value))
    print(math.cos(value))


if __name__ == '__main__':
    main()
```
## Результат.
<img width="895" height="445" alt="Image" src="https://github.com/user-attachments/assets/36a13576-35a2-4678-b252-dc09cb7e20c4" />

## Выводы
*Эта работа демонстрирует использование стандартного модуля math для выполнения математических операций. Программа показывает, как импортировать модуль и использовать его функции (квадратный корень, синус, косинус) для обработки пользовательского ввода. Это упражнение помогает понять, как стандартные библиотеки расширяют возможности языка и как их использовать для решения математических задач без реализации алгоритмов с нуля.*

## Лабораторная работа №9
*Напишите программу, которая будет рассчитывать какой день недели будет через n-нное количество дней, которые укажет пользователь.*
```python
from datetime import datetime as dt
from datetime import timedelta as td



def main():
    print(
        f"Сегодня {dt.today().date()}."
        f"День недели - {dt.today().isoweekday()}"
    )
    n = int(input('Введите количество дней: '))
    today = dt.today()
    result = today + td(days=n)
    print(
        f"Через {n} дней будет {result.date()}."
        f"День недели - {result.isoweekday()}"
    )


if __name__ == '__main__':
    main()
```
## Результат.
<img width="905" height="467" alt="Image" src="https://github.com/user-attachments/assets/92fe06eb-0759-4b73-bc4a-35e8f323b987" />

## Выводы
*Это задание демонстрирует использование стандартного модуля datetime для работы с датами и временем. Программа показывает, как определять текущую дату, вычислять дату в будущем и определять день недели. Это упражнение помогает понять, как стандартные библиотеки предоставляют мощные инструменты для работы со сложными типами данных, такими как даты и время, что является неотъемлемой частью многих реальных приложений.*

## Лабораторная работа №10
*Напишите программу с использованием глобальных переменных, которая будет считать площадь треугольника или прямоугольника в зависимости от того, что выберет пользователь. Получение всей необходимой информации реализовать через input(), а подсчет площадей выполнить при помощи функций. Результатом программы будет число, равное площади, необходимой фигуры.*
```python
global result


def rectangle():
    a = float(input("Ширина: "))
    b = float(input("Высота: "))
    global result
    result = a * b


def triangle():
    a = float(input("Основание: "))
    h = float(input("Высота: "))
    global result
    result = 0.5 * a * h


figure = input("1-прямоугольник, 2-треугольник: ")

if figure == '1':
    rectangle()
elif figure == '2':
    triangle()

print(f"Площадь: {result}")
```
## Результат.
<img width="801" height="428" alt="Image" src="https://github.com/user-attachments/assets/e176969f-96d7-4cf5-a393-5f4ced2f8948" />

<img width="895" height="544" alt="Image" src="https://github.com/user-attachments/assets/075e7238-e5ab-4054-b11b-8d983698cdc4" />

## Выводы
*Эта работа демонстрирует использование глобальных переменных для обмена данными между функциями. Программа показывает, как можно организовать вычисление площади разных фигур с использованием общей переменной для хранения результата.*

## Самостоятельная работа №1
*Дайте подробный комментарий для кода, написанного ниже. Комментарий нужен для каждой строчки кода, нужно описать что она делает. Не забудьте, что функции комментируются по-особенному.*
```python
# Импортируем класс datetime из модуля datetime для работы с датой и временем
from datetime import datetime

# Импортируем функцию sqrt (квадратный корень) из модуля math для математических вычислений
from math import sqrt


def main(**kwargs):
    """
    Главная функция для вычисления евклидова расстояния от начала координат до точек.

    Функция принимает произвольное количество именованных аргументов, где каждый аргумент
    представляет собой список из двух чисел [x, y], определяющих координаты точки на плоскости.

    Для каждой точки вычисляется расстояние по формуле: sqrt(x² + y²)

    Аргументы:
        **kwargs: произвольные именованные аргументы формата имя_точки=[x, y]

    Возвращает:
        None (результаты выводятся напрямую в консоль)
    """

    # Итерируемся по всем элементам словаря kwargs методом items()
    # Каждый элемент представляет собой кортеж (имя_точки, координаты)
    for key in kwargs.items():
        # Вычисляем евклидово расстояние от начала координат (0, 0) до точки (x, y)
        # key[1] - это список координат [x, y]
        # key[1][0] - координата x (первый элемент списка)
        # key[1][1] - координата y (второй элемент списка)
        # x**2 + y**2 - сумма квадратов координат
        # sqrt() - извлечение квадратного корня из суммы квадратов
        result = sqrt(key[1][0] ** 2 + key[1][1] ** 2)

        # Выводим вычисленное расстояние в консоль
        print(result)


# Проверяем, запущен ли скрипт напрямую (а не импортирован как модуль)
# Это стандартный паттерн в Python для разделения кода выполнения и кода импорта
if __name__ == '__main__':
    # Фиксируем текущее время начала выполнения программы
    # datetime.now() возвращает объект datetime с текущей датой и временем
    start_time = datetime.now()

    # Вызываем функцию main с передачей именованных аргументов
    # Каждый аргумент представляет собой точку с координатами [x, y]
    # one=[10, 3]   - точка с именем "one" и координатами x=10, y=3
    # two=[5, 4]    - точка с именем "two" и координатами x=5, y=4
    # three=[15, 13] - точка с именем "three" и координатами x=15, y=13
    # four=[93, 53]  - точка с именем "four" и координатами x=93, y=53
    # five=[133, 15] - точка с именем "five" и координатами x=133, y=15
    main(
        one=[10, 3],
        two=[5, 4],
        three=[15, 13],
        four=[93, 53],
        five=[133, 15]
    )

    # Вычисляем время выполнения программы как разницу между текущим временем и временем старта
    # datetime.now() - start_time возвращает объект timedelta, представляющий продолжительность
    time_costs = datetime.now() - start_time

    # Выводим время выполнения программы в консоль с помощью f-строки
    # Переменная time_costs автоматически преобразуется в читаемый формат (часы:минуты:секунды.микросекунды)
    print(f"Время выполнения программы - {time_costs}")
```
## Результат.
<img width="974" height="591" alt="Image" src="https://github.com/user-attachments/assets/388e1579-2ca4-43f2-8921-948537aeccc4" />

## Выводы
*Эта работа демонстрирует навык написания подробных комментариев к коду, включая документирование функций. Программа использует модули datetime и math для вычисления евклидова расстояния и замера времени выполнения. Это упражнение показывает важность комментирования кода для улучшения его читаемости и поддерживаемости.*

## Самостоятельная работа №2
*Напишите программу, которая будет заменять игральную кость с 6 гранями. Если значение равно 5 или 6, то в консоль выводится «Вы победили», если значения 3 или 4, то вы рекурсивно должны вызвать эту же функцию, если значение 1 или 2, то в консоль выводится «Вы проиграли». При этом каждый вызов функции необходимо выводить в консоль значение “кубика”. Для выполнения задания необходимо использовать стандартную библиотеку random. Программу нужно написать, используя одну функцию и “точку входа”.*
```python
import random


def roll_dice():
    dice_value = random.randint(1, 6)

    print(f"Значение кубика: {dice_value}")

    if dice_value == 5 or dice_value == 6:
        print("Вы победили")
    elif dice_value == 3 or dice_value == 4:
        print("Повторный бросок...")
        roll_dice()
    else:
        print("Вы проиграли")


if __name__ == '__main__':
    roll_dice()
```
## Результат.
<img width="865" height="520" alt="Image" src="https://github.com/user-attachments/assets/9111e694-2751-4092-ae4c-67e4d37df2ab" />

<img width="865" height="525" alt="Image" src="https://github.com/user-attachments/assets/96ad5938-f858-405d-b3e8-9e0772920541" />

## Выводы
*Эта работа демонстрирует использование рекурсии и стандартной библиотеки random для имитации броска игральной кости. Это упражнение помогает понять концепцию рекурсии, работу со случайными числами и организацию логики программы с ветвлением на основе случайных событий.*

## Самостоятельная работа №3
*Напишите программу, которая будет выводить текущее время, с точностью до секунд на протяжении 5 секунд. Программу нужно написать с использованием цикла. Подсказка: необходимо использовать модуль datetime и time, а также вам необходимо как-то “усыплять” программу на 1 секунду.*
```python
import datetime
import time


def print_current_time_for_5_seconds():
    for i in range(5):
        current_time = datetime.datetime.now().strftime("%H:%M:%S")
        print(current_time)
        time.sleep(1)


if __name__ == '__main__':
    print_current_time_for_5_seconds()
```
## Результат.
<img width="974" height="582" alt="Image" src="https://github.com/user-attachments/assets/21f11b94-2fb2-480b-842c-53c2a7069d4b" />

## Выводы
*Эта работа демонстрирует использование модулей datetime и time для работы с текущим временем и задержками в программе. Программа показывает, как получить текущее время, отформатировать его и приостановить выполнение программы на заданное время.*

## Самостоятельная работа №4
*Напишите программу, которая считает среднее арифметическое от аргументов вызываемое функции, с условием того, что изначальное количество этих аргументов неизвестно. Программу необходимо реализовать используя одну функцию и “точку входа”.*
```python
def calculate_average(*args):
    if len(args) == 0:
        return 0

    total = sum(args)
    average = total / len(args)
    return average


if __name__ == '__main__':
    # Вычисляем и выводим среднее арифметическое для различных наборов чисел

    # Пример с тремя числами
    result1 = calculate_average(1, 2, 3)
    print(f"Среднее арифметическое для чисел 1, 2, 3: {result1}")

    # Пример с четырьмя числами
    result2 = calculate_average(10, 20, 30, 40)
    print(f"Среднее арифметическое для чисел 10, 20, 30, 40: {result2}")

    # Пример с одним числом
    result3 = calculate_average(42)
    print(f"Среднее арифметическое для числа 42: {result3}")

    # Пример без аргументов
    result4 = calculate_average()
    print(f"Среднее арифметическое без аргументов: {result4}")
```
## Результат.
<img width="819" height="384" alt="Image" src="https://github.com/user-attachments/assets/8a748f4d-c158-49b0-8f57-772ee2a6d462" />

## Выводы
*Эта работа демонстрирует создание функции с переменным числом аргументов для вычисления среднего арифметического. Это упражнение помогает понять, как создавать гибкие функции, способные обрабатывать разное количество входных данных.*

## Самостоятельная работа №5
*Создайте два Python файла, в одном будет выполняться вычисление площади треугольника при помощи формулы Герона (необходимо реализовать через функцию), а во втором будет происходить взаимодействие с пользователем (получение всей необходимой информации и вывод результатов). Напишите эту программу и выведите в консоль полученную площадь.*
## Первый файл (Tema4_sam5_Triangle.py):
```python
import math


def calculate_triangle_area(a, b, c):
    """
    Вычисляет площадь треугольника по формуле Герона.

    Аргументы:
    a, b, c -- длины сторон треугольника

    Возвращает:
    Площадь треугольника или None, если стороны не образуют треугольника
    """
    # Проверяем неравенства треугольника
    if a + b <= c or a + c <= b or b + c <= a:
        return None

    # Вычисляем полупериметр
    p = (a + b + c) / 2

    # Вычисляем площадь по формуле Герона
    area = math.sqrt(p * (p - a) * (p - b) * (p - c))

    return area
```
## Второй файл (Tema4_sam5_main.py):
```python
from Tema4_sam5_Triangle import calculate_triangle_area


def get_valid_input(prompt):
    """
    Получает корректный числовой ввод от пользователя.

    Аргументы:
    prompt -- текст приглашения для ввода

    Возвращает:
    Введенное число (положительное)
    """
    while True:
        try:
            value = float(input(prompt))
            if value <= 0:
                print("Значение должно быть положительным числом. Попробуйте еще раз.")
                continue
            return value
        except ValueError:
            print("Некорректный ввод. Пожалуйста, введите число.")


def main():
    print("Вычисление площади треугольника по формуле Герона")
    print("------------------------------------------------")

    # Получаем длины сторон от пользователя
    a = get_valid_input("Введите длину первой стороны: ")
    b = get_valid_input("Введите длину второй стороны: ")
    c = get_valid_input("Введите длину третьей стороны: ")

    # Вычисляем площадь
    area = calculate_triangle_area(a, b, c)

    # Выводим результат
    if area is None:
        print("\nОшибка: введенные значения не могут быть сторонами треугольника.")
        print("Сумма любых двух сторон должна быть больше третьей стороны.")
    else:
        print(f"\nПлощадь треугольника: {area:.2f} кв. ед.")


if __name__ == "__main__":
    main()
```
## Результат.
<img width="974" height="689" alt="Image" src="https://github.com/user-attachments/assets/d30baa89-96df-46d3-a9a7-3edfb9cd1cee" />

<img width="974" height="581" alt="Image" src="https://github.com/user-attachments/assets/4114b678-c632-4f51-8c98-74ab853a1035" />

## Выводы
*Эта работа демонстрирует модульный подход к программированию через разделение кода на два файла: один для вычислений, другой для взаимодействия с пользователем. Программа показывает, как организовать валидацию входных данных, проверку условий существования треугольника и корректное использование импорта. Это упражнение помогает понять принципы модульности и разделения ответственности, что является основой для разработки структурированных и поддерживаемых приложений.*

## Общие выводы по теме
*Изученные в данной теме функции и стандартные модули представляют собой ключевые элементы программирования на Python, обеспечивающие структурированность кода и доступ к широкому спектру готовых решений.

Функции являются основным строительным блоком программирования, позволяющим:

- Устранять дублирование кода через повторное использование
- Упрощать отладку за счет изоляции логики
- Создавать модульную структуру программы
- Реализовывать сложные алгоритмы через комбинацию простых функций
- Поддерживать читаемость кода через осмысленное именование и документирование
- Особое внимание следует уделить различным способам передачи аргументов в функции:
  - Переменное количество аргументов через *args
  - Переменное количество именованных аргументов через **kwargs

Стандартные библиотеки Python предоставляют огромный набор готовых решений для самых различных задач:

- Модуль math предоставляет математические функции и константы
- Модуль datetime позволяет работать с датами и временем
- Модуль random обеспечивает генерацию случайных чисел
- Модуль time предоставляет функции для работы с временными задержками

Практическая ценность изученного материала проявляется во многих аспектах программирования:

- Упрощение разработки за счет использования готовых решений из стандартной библиотеки
- Создание структурированного и поддерживаемого кода через модульность
- Возможность написания универсальных функций, работающих с различными наборами данных
- Повышение производительности разработки за счет повторного использования кода

Знание и умение применять функции и стандартные модули Python являются обязательными навыками для любого программиста на этом языке, так как они формируют основу для разработки эффективных, читаемых и надёжных программ. Изученные в данной теме концепции являются фундаментом для освоения более сложных аспектов программирования на Python.*
